// Problem 20

class Solution {
public:
  bool isValid(string s) {
    bool ans = false;

    stack<char> brackets;

    for (int i = 0; i < s.length(); i++) {
      if (s[i] == "(" || s[i] == "[" || s[i] == "{") {

      }
    }
  }
};